<div class="card mb-5">
    <div class="card-header"  style="background-color:#444590;">
        {{!-- <h4 class="card-title"><a class="text-light" href="/post/{{id}}">{{title}}</a></h4> --}}
        <h5class="mb-2 text-light">{{post_title}}</h5>
        <h6 class="mb-2 text-light">Posted by {{user.name}} on {{format_date created_at}} {{format_time created_at}}</h6>
    </div>
    <div>
        <p class="card-text">{{post_content}}</p>
    </div>
</div>
